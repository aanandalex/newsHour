<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <title>Article</title>
  </head>
  <body onload="startTime()">
    <div class="container-fluid">
      <div class="row" id="navbar">
        <div class="col-md-3">
          <h1 class="" id="menu">
            <a href="/article" id="menuStyle">Menu</a>
          </h1>
        </div>
        <div class="col-md-6">
            <h1>
              <a href="/article" id="menuStyle" style="text-align: center !important;font-weight: 600; font-size: 30px;">B-W</a>
            </h1>
          </div>
        <div class="col-md-3">
          <h1 class="" id="clock"></h1>
        </div>
        
      </div>
    </div>
<style>
 body::-webkit-scrollbar {
  display: none;
}
  body {
    color: #1f1f1f;
  }
  #navbar {
   width: 100%;
   width: 100vw;
   padding: 30px;
   position: fixed;
   top: 0;
   right: 0;
   z-index: 3;
  }
  #menu {
    text-align: left;
    font-weight: 600;
    font-size: 30px;
  }
  #menuStyle {
    color: #1f1f1f;
    /* text-transform: uppercase; */
    text-decoration: none;
    /* letter-spacing: 0.15em; */
    display: inline-block;
    position: relative;
    margin-left: -30px;
  }
  #date {
    text-align: center;
    font-weight: 600;
    font-size: 30px;
    /* text-transform: uppercase; */
    letter-spacing: 0.15em;
  }
  #clock {
    text-align: right;
    font-weight: 600;
    font-size: 30px;
    /* text-transform: uppercase; */
  }
  #brainwash {
    font-weight: 500;
    text-align: center;
    font-size: 275px;
    text-transform: uppercase;
    margin-top: 100px;
    margin-left: 5px;
    line-height: 90%;
  }
  #articles {
            font-weight: 700;
            text-align: center;
            font-size: 16px;
            text-transform: uppercase;
            /* letter-spacing: 0.25em; */
            margin-top: 150px;
            /* padding-left: 60px; */
            line-height: 80%;
        }
        hr {
            border: none;
            border-top: 2px solid #1f1f1f;
            color: #1f1f1f;
            overflow: visible;
            text-align: center;
            height: 0.5px;
            margin-right: 30px;
            width: 95vw;
        }
        #menuStyle:after {    
            background: none repeat scroll 0 0 transparent;
            bottom: 0;
            content: "";
            display: block;
            height: 2px;
            /* left: 50%; */
            right: 0;
            position: absolute;
            background: #1f1f1f;
            /* transition: width 0.3s ease 0s, left 0.3s ease 0s; */
            transition: width 0.5s ease 0s, right 0.3s ease 0s;
            width: 0;
        }
        #menuStyle:hover:after { 
          width: 100%; 
          left: 0;
        }
        #menuStyle:hover {
          color: #1f1f1f;
        }
        body{
        font-family: 'Montserrat', sans-serif;
        background-color: #f7f7f5;
        }
        h1 {
            font-weight: 700;
            text-align: center;
        }
        .list-group-item {
            border: none !important;
            font-weight: 300;
        }
        #menuStyle {
            text-decoration: none !important;
            color: black;
        }
</style>
<script>
  function startTime() {
    var today = new Date();
    var hr = today.getHours();
    var min = today.getMinutes();
    var sec = today.getSeconds();
    ap = (hr < 12) ? "<span>AM</span>" : "<span>PM</span>";
    hr = (hr == 0) ? 12 : hr;
    hr = (hr > 12) ? hr - 12 : hr;
    //Add a zero in front of numbers<10
    hr = checkTime(hr);
    min = checkTime(min);
    sec = checkTime(sec);
    document.getElementById("clock").innerHTML = hr + " : " + min + " " + ap;
    var time = setTimeout(function(){ startTime() }, 500);
}
function checkTime(i) {
    if (i < 10) {
        i = "0" + i;
    }
    return i;
}
</script>
  